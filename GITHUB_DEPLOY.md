# دليل النشر على GitHub Actions (عبر المتصفح) 🚀

هذا الدليل مخصّص لتشغيل الـ workflow تلقائياً على السحابة **مجاناً وطوال الوقت** — دون الحاجة لإبقاء جهازك مشغّلاً. سنستخدم متصفح الويب فقط (بدون تثبيت أي برنامج).

> ⏱️ الوقت المتوقّع: نحو 10 دقائق لإعداد GitHub + تيليغرام. أمّا مفاتيح فيسبوك/إنستغرام/تيك توك فتأخذ وقتاً إضافياً (بعضها يتطلب مراجعة وموافقة).

---

## نصيحة ذكية للبدء ⭐

الـ workflow مصمّم بذكاء: **كل منصة تُفعَّل تلقائياً فقط عندما تضيف سرّها (Secret)**. لذلك:

> ابدأ بإضافة سرّي **تيليغرام فقط** الآن → سترى أول منشور خلال دقائق. ثم أضِف مفاتيح فيسبوك/إنستغرام/تيك توك لاحقاً كلما حصلت عليها، وستنضم تلقائياً دون تعديل أي كود.

الحد الأدنى للتشغيل: `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` (وحتى بدون مفتاح يوتيوب سيجمع الترندات من Google Trends).

---

## الخطوة 1 — نزّل حزمة المشروع وفكّ ضغطها

1. نزّل ملف **`algeria-trends-workflow.zip`** (المرفق في المحادثة).
2. فُكّ الضغط (Extract) — ستحصل على مجلد `algeria-trends-workflow`.
3. تأكّد أنك ترى بداخله مجلد `.github` (مهم جداً — هو الذي يشغّل الأتمتة).

---

## الخطوة 2 — أنشئ مستودعاً خاصاً (Private repo)

1. ادخل إلى https://github.com/new
2. **Repository name:** مثلاً `algeria-trends`.
3. اختر **Private** (خاص) — لحماية إعداداتك.
4. **لا** تضِف README أو .gitignore (لدينا بالفعل).
5. اضغط **Create repository**.

---

## الخطوة 3 — ارفع الملفات (سحب وإفلات)

1. في صفحة المستودع الجديد، اضغط الرابط **uploading an existing file**
   (أو **Add file → Upload files**).
2. افتح مجلد `algeria-trends-workflow` المفكوك، **حدّد كل محتوياته** (Ctrl+A) بما فيها مجلد `.github`.
3. اسحبها وأفلتها في صفحة GitHub.
4. انتظر حتى تُرفع كل الملفات (تأكّد أن `.github/workflows/publish.yml` ظهر ضمن القائمة).
5. اكتب رسالة commit بسيطة مثل `initial upload` ثم اضغط **Commit changes**.

> 💡 في ويندوز يظهر مجلد `.github` بشكل طبيعي في مستكشف الملفات، فلا تقلق.

---

## الخطوة 4 — أضِف الأسرار (Secrets) 🔐

من صفحة المستودع: **Settings → Secrets and variables → Actions → New repository secret**.
أضِف كل سرّ باسمه بالضبط (حسّاس لحالة الأحرف):

| اسم السرّ (Secret) | للمنصة | من أين تحصل عليه |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | تيليغرام ✅ | من **@BotFather** بعد الأمر `/newbot` |
| `TELEGRAM_CHAT_ID` | تيليغرام ✅ | اسم قناتك مثل `@my_channel` أو معرّف `-100...` |
| `YOUTUBE_API_KEY` | جمع الترندات | Google Cloud Console → *YouTube Data API v3* → API key |
| `FB_PAGE_ID` | فيسبوك | معرّف صفحتك (`GET /me/accounts`) |
| `FB_PAGE_TOKEN` | فيسبوك | توكن **صفحة** طويل الأمد بصلاحيات النشر |
| `IG_USER_ID` | إنستغرام | `GET /{page_id}?fields=instagram_business_account` |
| `IG_TOKEN` | إنستغرام | توكن طويل الأمد بصلاحية `instagram_content_publish` |
| `TIKTOK_TOKEN` | تيك توك | `access_token` من تطبيق TikTok المعتمد |

> 📖 خطوات الحصول على كل مفتاح مشروحة بالتفصيل في ملف **`SETUP_GUIDE.md`** داخل المشروع.
> يمكنك إضافة أسرار تيليغرام الآن وترك الباقي لوقت لاحق.

---

## الخطوة 5 — شغّل أول مرة يدوياً

1. اذهب إلى تبويب **Actions** في المستودع.
2. إن ظهرت رسالة لتفعيل الـ workflows اضغط **I understand my workflows, go ahead and enable them**.
3. اختر workflow باسم **«نشر ترندات الجزائر تلقائياً»** من القائمة اليمنى.
4. اضغط **Run workflow → Run workflow** (تشغيل يدوي فوري للاختبار).
5. انتظر دقيقة، ثم افتح التشغيل لرؤية السجلّات.

إن نجح، ستجد أول منشور في قناتك على تيليغرام فوراً 🎉

---

## الخطوة 6 — الجدولة التلقائية ⏰

بعد نجاح التشغيل اليدوي، يعمل النظام تلقائياً **كل 6 ساعات** (4 مرات يومياً).

لتغيير التكرار: عدّل السطر التالي في ملف `.github/workflows/publish.yml`:

```yaml
- cron: '0 */6 * * *'     # كل 6 ساعات
# أمثلة:
# '0 */3 * * *'           # كل 3 ساعات (8 مرات يومياً)
# '0 8,14,20 * * *'       # 3 مرات: 8ص، 2م، 8م (بتوقيت UTC)
```

> ⚠️ التوقيت في cron بتوقيت **UTC**. الجزائر = UTC+1، فاطرح ساعة عند الحساب.

---

## ملاحظات مهمة

- **الحصص المجانية:** المستودعات الخاصة تحصل على ~2000 دقيقة/شهر مجاناً، وكل تشغيلة تستهلك دقيقة تقريباً — أي أنك في أمان تام.
- **الخمول:** يوقف GitHub الجدولة تلقائياً بعد **60 يوماً** من عدم النشاط في المستودع. أي تعديل بسيط يعيد تفعيلها.
- **منع التكرار:** يحفظ النظام ما نُشر سابقاً (عبر ذاكرة تخزين مؤقتة) لتجنّب تكرار نفس الترند.

---

## استكشاف أخطاء Actions

| ما تراه | الحل |
|---|---|
| التشغيل أخضر لكن لا منشور | راجع السجلّات: تأكّد أن أسرار تيليغرام صحيحة وأن البوت **مشرف** في القناة. |
| خطأ في تيليغرام | `chat_id` خاطئ، أو البوت ليس مشرفاً بصلاحية النشر. |
| «تم تخطّي يوتيوب» | لم تُضِف `YOUTUBE_API_KEY` (اختياري — Google Trends يعمل بدونه). |
| فيسبوك/إنستغرام يفشل | التوكن منتهٍ أو ينقصه صلاحية — جدّده حسب `SETUP_GUIDE.md`. |
| لا يعمل تلقائياً | تأكّد أن الـ workflow مفعّل في تبويب Actions وأن المستودع نشط. |

جاهز؟ ابدأ بالخطوة 1، وأنا معك في أي خطوة تتعثّر فيها. 💪
